<%= form_for([@stock, @stock.comments.build], :class=>"pure-form pure-form-stacked") do |f| %>
    <fieldset>
      <%= f.label :commenter %>
      <%= f.text_field :commenter %>
      <%= f.label :body %>
      <%= f.text_area :body %>
      <%= f.submit %>
    </fieldset>
<% end %>